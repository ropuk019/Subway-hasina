{
  "name": "hasina-runner",
  "version": "1.0.0",
  "description": "An endless runner game featuring Hasina",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android": "npm run build && npx cap sync && npx cap open android",
    "ios": "npm run build && npx cap sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^5.5.0",
    "@capacitor/core": "^5.5.0",
    "@capacitor/ios": "^5.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.0",
    "vite": "^5.0.0"
  }
}